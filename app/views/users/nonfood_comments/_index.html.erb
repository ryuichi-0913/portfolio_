          <thead>
            <tr>
              <th colspan="3"></th>
            </tr>
          </thead>
        <% @nonfood.nonfood_comments.each do |nonfood_comment| %>
          <tbody>
              <tr>
                <td><%= link_to users_user_path(nonfood_comment.user), class: 'hoge' do %>
                      <%= attachment_image_tag nonfood_comment.user, :user_image, size: '50x50',　format: 'jpeg', fallback: "no_image.jpg" %><br>
                      <%= nonfood_comment.user.name %>
                    <% end %></td>
                <td><%= nonfood_comment.created_at.strftime('%Y/%m/%d %H:%M:%S') %><br>
                  <p><%= nonfood_comment.nonfood_comment %></p></td>
                <td><% if nonfood_comment.user == current_user %>
                  　 <%= link_to "削除", users_nonfood_nonfood_comment_path(nonfood_comment.nonfood, nonfood_comment), method: :delete, remote: true, class: "btn btn-danger pull-right" %>
                    <% end %></td>
              </tr>
          </tbody>
        <% end %>